public class PlaneShadow : global::UnityEngine.MonoBehaviour
{
    private global::UnityEngine.Vector3 lightDir;
    private float lenY;
    private bool bPlayer;
    public void Init(global::UnityEngine.Vector3 light, bool isPlayer)
    {
    }

    private void Update()
    {
    }
}