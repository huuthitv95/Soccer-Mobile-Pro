public class MainViewLoader : global::UnityEngine.MonoBehaviour
{
    public enum MainViewButtonType
    {
        PlayerCareer = 0,
        MasterLeague = 1,
        Friendly = 2,
        Shop = 3,
        Training = 4
    }

    private static global::UnityEngine.GameObject proto;
    private void Awake()
    {
    }

    private void LoadForPlayerCareer(MainView mainViewScript, global::UnityEngine.Transform mountNode)
    {
    }

    private void LoadForlegacy(MainView mainViewScript, global::UnityEngine.Transform mountNode)
    {
    }
}